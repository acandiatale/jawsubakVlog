import introduce from './components/introduce'
import vlog from './components/vlog'

let routes = [
    {path: "/introduce", name: "Introduce", component: introduce},
    {path: "/board", name: "Vlog", component: vlog}
]

export default routes
